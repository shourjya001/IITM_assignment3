apiVersion: v1
kind: Service
metadata:
  name: web-dashboard-service
spec:
  type: NodePort
  selector:
    app: iitm-web
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080
